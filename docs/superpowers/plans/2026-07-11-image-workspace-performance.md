# Image Workspace Performance Plan

1. Record the `/app/image` production-build baseline and inspect its eager modal/settings imports.
2. Add a failing architecture contract for deferred image modals and advanced fields.
3. Dynamically load the auth gate and Library modal only under their existing open-state conditions.
4. Split the advanced-settings fields from the always-visible toggle and load them on expansion.
5. Run focused image contracts, TypeScript, lint, exposure checks, and a production build.
6. Compare the route bundle and smoke-test the default workspace, Advanced settings, Library, and auth paths.
